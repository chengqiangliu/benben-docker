# cluster.name: "benben-elasticsearch-cluster"
network.host: 0.0.0.0
node.name: "benben-node1"
cluster.initial_master_nodes: "benben-node1"
# discovery.type: "single-node"
# Path to directory where to store the data (separate multiple locations by comma):
path.data: /opt/elasticearch/benben-node1/data
# Path to log files:
path.logs: /opt/elasticearch/benben-node1/logs
transport.tcp.port: 9300
http.port: 9200
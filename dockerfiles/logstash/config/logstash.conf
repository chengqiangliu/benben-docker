input {
  beats {
    port => 5044
    type => beats
  }
}

filter {
  # GPP apps logs
  if [type] == "supervisor" or "benben_app" in [tags]{

  }
}

output {
  elasticsearch {
    hosts => ['host.elasticsearch.benben:9200']
    index => "filebeat-%{+YYYY.MM.dd}"
  }
}